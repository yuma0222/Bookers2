<h2>User info</h2>
<%= attachment_image_tag @user, :profile_image,　format: 'jpeg', fallback: "no_image.jpg", size: "50x50" %><table class="table">
  <tbody><tr></tr>
      <tr>
          <th>name</th>
          <th><%= @user.name %></th>
      </tr>
      <tr>
          <th>introduction</th>
          <th><%= @user.introduction %></th>
      </tr>
  </tbody></table>

  <div class="row">
    <% if @user.id == current_user.id %>
    <p><%= link_to "Edit", edit_user_path(@user), class: "btn btn-primary col-xs-12" %></p>
    <% end %>
</div>

<h2>New book</h2>


<%= form_for(@book) do |f| %>
<h4>Title</h4>
<%= f.text_field :title %>
<h4>Opinion</h4>
<%= f.text_area :body %>

<div class="actions row" >
    <%= f.submit 'Create book' %>
</div>
<% end %>

</div>